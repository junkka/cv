%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Compact Academic CV
% LaTeX Template
% Version 1.0 (10/6/2012)
%
% This template has been downloaded from:
% http://www.LaTeXTemplates.com
%
% Original author:
% Dario Taraborelli (http://nitens.org/taraborelli/home)
%
% License:
% CC BY-NC-SA 3.0 (http://creativecommons.org/licenses/by-nc-sa/3.0/)
%
% Important:
% This template needs to be compiled using XeLaTeX
%
% Note: this template has the option to use the Hoefler Text font, see the
% font configurations section below for instructions on using this font
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%----------------------------------------------------------------------------------------
%	PACKAGES AND OTHER DOCUMENT CONFIGURATIONS
%----------------------------------------------------------------------------------------

\documentclass[11pt]{article} % Document font size and paper size

\usepackage{fontspec} % Allows the use of OpenType fonts

\usepackage{geometry} % Allows the configuration of document margins
\geometry{textwidth=5.5in, textheight=8.5in, marginparsep=7pt, marginparwidth=.6in} % Document margin settings
\setlength\parindent{0in} % Remove paragraph indentation

\usepackage[usenames,dvipsnames]{xcolor} % Custom colors

\usepackage{sectsty} % Allows changing the font options for sections in a document
\usepackage[normalem]{ulem} % Custom underlining
\usepackage{xunicode} % Allows generation of unicode characters from accented glyphs

\usepackage{url}
% url breaks
\expandafter\def\expandafter\UrlBreaks\expandafter{\UrlBreaks%  save the current one
  \do\a\do\b\do\c\do\d\do\e\do\f\do\g\do\h\do\i\do\j%
  \do\k\do\l\do\m\do\n\do\o\do\p\do\q\do\r\do\s\do\t%
  \do\u\do\v\do\w\do\x\do\y\do\z\do\A\do\B\do\C\do\D%
  \do\E\do\F\do\G\do\H\do\I\do\J\do\K\do\L\do\M\do\N%
  \do\O\do\P\do\Q\do\R\do\S\do\T\do\U\do\V\do\W\do\X%
  \do\Y\do\Z\do\0\do\1\do\2\do\3\do\4\do\5\do\6\do\7%
  \do\8\do\9}



\defaultfontfeatures{Mapping=tex-text} % Converts LaTeX specials (``quotes'' --- dashes etc.) to unicode

\usepackage{marginnote} % For margin years
\newcommand{\years}[1]{\marginnote{\scriptsize #1}} % New command for including margin years
\renewcommand*{\raggedleftmarginnote}{}
\setlength{\marginparsep}{7pt} % Slightly increase the distance of the margin years from the contant
\reversemarginpar

\usepackage[xetex, bookmarks, colorlinks, breaklinks, pdftitle={$author$ - vita},pdfauthor={$author$}]{hyperref} % PDF setup - set your name and the title of the document to be incorporated into the final PDF file meta-information
\hypersetup{linkcolor=blue,citecolor=blue,filecolor=black,urlcolor=MidnightBlue} % Link colors

\usepackage{wrapfig}

%----------------------------------------------------------------------------------------
%	FONT CONFIGURATIONS
%----------------------------------------------------------------------------------------

% Two font choices are available in this template, the default is Linux Libertine, available for free at: http://www.linuxlibertine.org while the secondary choice is Hoefler Text which comes bundled with Mac OSX.
% To use Hoefler Text, comment out the Linux Libertine block below and uncomment the Hoefler Text block. You will also need to replace the "\&" characters with "\amper{}" in section titles.

% Linux Libertine Font (default)
\setromanfont [Ligatures={Common}, Numbers={OldStyle}, Variant=01]{Linux Libertine O} % Main text font
%\setmonofont[Scale=0.8]{Monaco} % Set mono font (e.g. phone numbers)
\sectionfont{\mdseries\upshape\Large} % Set font options for sections
\subsectionfont{\mdseries\scshape\normalsize} % Set font options for subsections
\subsubsectionfont{\mdseries\upshape\large} % Set font options for subsubsections
\chardef\&="E050 % Custom ampersand character

% Hoefler Text Font (bundled with Mac OSX)
%\setromanfont [Ligatures={Common}, Numbers={OldStyle}]{Hoefler Text} % Main text font
%\setmonofont[Scale=0.8]{Monaco} % Set mono font (e.g. phone numbers)
%\setsansfont[Scale=0.9]{Optima Regular} % Set sans font, used in the main name and titles in the document
%\newcommand{\amper}{{\fontspec[Scale=.95]{Hoefler Text}\selectfont\itshape\&}} % Custom ampersand character
%\sectionfont{\sffamily\mdseries\large\underline} % Set font options for sections
%\subsectionfont{\rmfamily\mdseries\scshape\normalsize} % Set font options for subsections
%\subsubsectionfont{\rmfamily\bfseries\upshape\normalsize} % Set font options for subsubsections

%----------------------------------------------------------------------------------------

\begin{document}

%----------------------------------------------------------------------------------------
%	CONTACT AND GENERAL INFORMATION SECTION
%----------------------------------------------------------------------------------------

{\LARGE \em{$title$}}\\[0.5cm] % Your name
{\LARGE $author$}\\[1cm] % Your name

\begin{wrapfigure}{r}{0.27\textwidth}
  \vspace{-16pt}
  \includegraphics[width=0.25\textwidth]{assets/profile_pic.png}
\end{wrapfigure}

$info.adress1$\\ % Your address
$info.adress2$\\ % Your address
$info.adress3$\\ % Your address
$info.adress4$\\ % Your address
[.2cm]
Phone: \texttt{$info.phone$}\\ % Your phone number
Email: \href{mailto:#}{$info.email$}\\ % Your email address
\textsc{url}: \href{http://$info.url$}{$info.url$}\\ % Your academic/personal website


\vspace{40pt}


Born: $info.born$, $info.born-in$\\ % Your date of birth
Nationality: $info.nationality$ % Your nationality


$body$

\vspace{20pt}

%------------------------------------------------

$if(current.title)$
\section*{Current position}

\emph{$current.title$}, $current.university$ % Your current or previous employment position
$endif$
%------------------------------------------------

\section*{Research interests}

$info.interests$. % Your primary areas of research interest

%----------------------------------------------------------------------------------------
%	WORK EXPERIENCE SECTION
%----------------------------------------------------------------------------------------

\section*{Appointments}

$for(appointments)$
\years{$appointments.start$-$appointments.end$}$appointments.title$, $appointments.university$\\
$endfor$

%----------------------------------------------------------------------------------------
%	EDUCATION SECTION
%----------------------------------------------------------------------------------------

\section*{Education}

$for(degrees)$
\years{$degrees.year$}\textsc{$degrees.degree$} $degrees.university$ $degrees.place$\\
$endfor$


%----------------------------------------------------------------------------------------
%	PUBLICATIONS AND TALKS SECTION
%----------------------------------------------------------------------------------------

\vspace{10pt}

\section*{Publications}

\subsection*{Articles and books}

$for(references)$
$if(references.article-journal)$
  \years{$references.issued$}
    $for(references.author)$$references.author.family$, $references.author.given$$sep$, $endfor$
    ($references.issued$) “$references.title$". 
    \emph{$references.container-title$$if(references.volume)$, $references.volume$ $endif$}$if(references.issue)$ ($references.issue$)$endif$$if(references.page)$, $references.page$$endif$.
    $if(references.doi)$ \\doi:\url{http://dx.doi.org/$references.doi$}$endif$
    $if(references.URL)$ $references.URL$$endif$
$endif$
$if(references.book)$
  \years{$references.issued$}
    $for(references.author)$$references.author.family$, $references.author.given$$sep$, $endfor$
    ($references.issued$)
    \emph{$references.title$}.
    $if(references.publisher)$ $references.publisher$$endif$$if(references.adress)$: $references.adress$$endif$
    $if(references.URL)$  $references.URL$$endif$
$endif$

$endfor$


\subsection*{Working papers}

$for(references)$
$if(references.working-paper)$
  \years{$references.issued$}
    $for(references.author)$$references.author.family$, $references.author.given$$sep$, $endfor$
    ($references.issued$)
    \emph{$references.title$}.
    $if(references.URL)$  $references.URL$$endif$
$endif$

$endfor$
\subsection*{Software}

$for(references)$
$if(references.software)$
\years{$references.issued$}
  $for(references.author)$$references.author.family$, $references.author.given$$sep$, $endfor$ ($references.issued$) “$references.title$“ $references.URL$\\
$endif$
$endfor$

\vspace{20pt}

\section*{Conferences \& Invited presentations}

$for(talks)$
\years{$talks.year$} “$talks.title$“, \emph{$talks.event$}, $talks.place$. $if(talks.url)$$talks.url$$endif$\\
$endfor$


\section*{Workshops \& Post-graduate courses}

$for(workshops)$
\years{$workshops.year$}\textsc{$workshops.title$} $workshops.event$, $workshops.university$\\
$endfor$


%----------------------------------------------------------------------------------------
%	TEACHING SECTION
%----------------------------------------------------------------------------------------

\section*{Teaching}

$for(teaching)$
\years{$if(teaching.year)$ $teaching.year$ 
        $else$ $teaching.from$ - $teaching.to$
        $endif$}
\textsc{$teaching.course$} $if(teaching.program)$, $teaching.program$$endif$$if(teaching.place)$, $teaching.place$$endif$. $if(teaching.url)$$teaching.url$$endif$\\
$endfor$


\section*{Service to the profession}

$for(other)$
\years{$if(other.from)$
       $other.from$
       $endif$
       $if(other.to)$-$other.to$
       $endif$}
\textsc{$other.title$} $if(other.university)$$other.university$$endif$
$if(other.url)$ $other.url$$endif$\\
$endfor$


\section*{Affiliations}

$for(affiliations)$
\years{$affiliations.from$-$affiliations.to$}\textsc{$affiliations.title$} $affiliations.university$\\
$endfor$


\section*{Grants}

$for(grants)$
\years{$grants.year$}\textsc{$grants.institution$}, $grants.type$. $grants.amount$ \\
$endfor$


\section*{Language skills}

\subsection*{Languages}

$for(languages.languages)$
\textsc{$languages.languages.language$} ($languages.languages.level$)
$endfor$

\subsection*{Programming}

$for(languages.programming)$
\textsc{$languages.programming.language$} ($languages.programming.level$)
$endfor$


%------------------------------------------------

\vfill{} % Whitespace before final footer

%----------------------------------------------------------------------------------------
%	FINAL FOOTER
%----------------------------------------------------------------------------------------

\begin{center}
{\scriptsize Last updated: \today} 
\end{center}

%----------------------------------------------------------------------------------------

\end{document}